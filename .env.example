# ZapFlow — configuração de produção no Railway

# Configuração pública do Supabase (o backend também possui fallback oficial).
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Necessária para worker persistente e gravação de eventos do gateway.
# Nunca exponha esta chave no frontend.
SUPABASE_SERVICE_ROLE_KEY=

# Evolution API. Também pode ser configurada por organização na tela WhatsApp.
EVOLUTION_API_URL=
EVOLUTION_API_KEY=

# Segredos somente de servidor.
GATEWAY_WEBHOOK_SECRET=
CAMPAIGN_WORKER_SECRET=

# Espaçamento por sessão. O código nunca permite menos de 10 segundos.
CAMPAIGN_SEND_DELAY_MS=15000
